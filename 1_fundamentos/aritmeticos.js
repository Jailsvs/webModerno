const [numeroA, numeroB, numeroC, numeroD] = [1, 2, 3, 4];

const soma = numeroA + numeroB + numeroC + numeroD;
const subtracao = numeroD - numeroB;
const multiplicacao = numeroA * numeroB;
const divisao = numeroD / numeroA;
const modulo = numeroD % 2; 
console.log(soma, subtracao, multiplicacao, divisao, modulo);